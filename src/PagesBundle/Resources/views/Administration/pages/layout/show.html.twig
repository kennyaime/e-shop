{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
    <h1>Pages</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ page.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ page.contenu }}</td>
            </tr>
        </tbody>
    </table>


            <a href="{{ path('adminPages_index') }}">Retour à la liste</a><br />

            <a href="{{ path('adminPages_edit', { 'id': page.id }) }}">Modifier</a><br/>

            {{ form_start(delete_form) }}
                <button class="btn btn-default" type="submit" value="Supprimer">Supprimer</button>
            {{ form_end(delete_form) }}

{% endblock %}
